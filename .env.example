# PostgreSQL local
POSTGRES_DB=finan
POSTGRES_USER=finan
# Preencha somente no .env local com uma senha forte e exclusiva.
POSTGRES_PASSWORD=
POSTGRES_PORT=5432
BACKEND_PORT=8080

# URL usada pelo backend dentro do Docker Compose
DATABASE_URL=jdbc:postgresql://postgres:5432/${POSTGRES_DB}
