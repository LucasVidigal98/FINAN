# PostgreSQL local
POSTGRES_DB=finan
POSTGRES_USER=finan
# Preencha somente no .env local com uma senha forte e exclusiva.
POSTGRES_PASSWORD=
POSTGRES_PORT=5432
BACKEND_PORT=8080

# URL usada pelo backend
DATABASE_URL=jdbc:postgresql://localhost:${POSTGRES_PORT}/finan
